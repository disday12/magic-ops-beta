services:
  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    volumes:
      - disney_ops_weather_data:/data

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  disney_ops_weather_data: